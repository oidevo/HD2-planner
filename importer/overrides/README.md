# Manual overrides

Overrides are applied after structured import and receive `manual_override` provenance. Use them only for facts unavailable or awkwardly represented in the wiki APIs, or for explicitly subjective planner tags. Never remove imported provenance. Planner tags must be objects with an `origin` and should include a confidence level.

